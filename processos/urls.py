# from django.urls import path
# from . import views

# app_name = 'processos'

# urlpatterns = [
#     path('lista-processos/', views.ProcessoListView.as_view(), name='list'),
#     path('novo/', views.ProcessoCreateView.as_view(), name='create'),
#     path('<int:pk>/detalhe/', views.ProcessoDetailView.as_view(), name='detail'),
#     path('<int:pk>/editar/', views.ProcessoUpdateView.as_view(), name='processo-update'),
#     path('<int:pk>/excluir/', views.ProcessoDeleteView.as_view(), name='delete'),
# ]
